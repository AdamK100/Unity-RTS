using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using System.Linq;
using UnityEngine.UI;
public class slot : MonoBehaviour
{
    public Player p;
    public void OnClick()
    {
        /*GameObject objectt;
        objectt = Selection.selected[0].GetComponent<Building>().queue[ButtonScript.gameManager.GetComponent<UI>().queueImages.ToList().IndexOf(GetComponent<Image>())];
        if (ButtonScript.gameManager.GetComponent<UI>().queueImages.ToList().IndexOf(GetComponent<Image>()) == 0)
        {
            Selection.selected[0].GetComponent<Building>().timer = 0;
        }
        Selection.selected[0].GetComponent<Building>().queue.Remove(objectt);
        p.money += objectt.GetComponent<Tank>().price;*/
    }
}
